#include<iostream>
using namespace std;
int main(){
    int n;
    cin>>n;
    int arr[n];
    for(int i=0;i<n;i++){
        cin>>arr[i];
    }
    for(int i=0;i<n-1;i++){
        bool flag=false;
        for(int j=0;j<n-1;j++){
                // for decreasing change the comparison relation
            if(arr[j]>arr[j+1]){
                flag=true;
                swap(arr[j],arr[j+1]);
            }
        }
        if(flag==false)break;
    }
    for(int i=0;i<n;i++){
        cout<<arr[i]<<"  ";
    }
    return 0;
}
